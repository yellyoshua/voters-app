import { ReactElement } from "react";
import styled from "@emotion/styled";

const IdukayLogin = styled.div``;

const createLoginSession = (_token: string) => { };

export default (): ReactElement => {
  return <IdukayLogin>

  </IdukayLogin>
}